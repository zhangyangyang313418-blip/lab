# MLA Test Flow Chart Standalone Notes

- Output file: `docs/MLA_test_flow_chart_standalone.xlsx`
- Sheet `Flow Chart`: standalone overview with header summary, baseline block, Group A/B/C detail blocks, and Group D parallel overview cards.
- Sheet `Timing Summary`: one-line subtotal per group for quick planning review.
- Sheet `Detailed List`: flattened full list of all included tests with sample scope, time in days, and basis/note.

Current timing rules used:

- Baseline optical test: 7 days
- Post-group optical test: 2 days per group
- L1/L4: Group A = 3 days; other groups = 2 days
- Remaining uncertain durations were aligned to `JLR L481 PV  test timing plan-4.10.xls`
- EMC remains `TBD`

Open items to review:

- K20 Solar Radiation currently follows L481 timing plan instead of pure standard-duration math
- K21 Corrosive Gases currently follows L481 timing plan instead of pure standard-duration math
- K23 Thermal Shock Endurance currently follows L481 timing plan instead of pure standard-duration math
- D8 HALT remains `No Test` because the source MLA flow chart marks it that way
